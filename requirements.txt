# uttera-tts-hotcold — base dependencies (backend-agnostic)
#
# These are required regardless of which TTS backend you choose.
# After installing these, install ONE backend-specific requirements file:
#
#   pip install -r requirements-coqui.txt      # Coqui XTTS-v2 (default)
#   pip install -r requirements-voxcpm.txt     # VoxCPM2 (coming soon)
#
# Or for legacy single-file install (Coqui only):
#   pip install -r requirements-coqui.txt
#   (it includes -r requirements.txt internally)

fastapi
uvicorn
pydantic>=2.0
python-multipart
python-dotenv
redis[asyncio]>=5.0.0

# Prometheus /metrics endpoint — uses the default global registry.
# Scrape with Telegraf's inputs.prometheus, Prometheus itself, or any
# OpenMetrics-compatible consumer.
prometheus-client>=0.20.0
